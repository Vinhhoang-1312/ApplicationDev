@model IEnumerable<StaffTrainee.Models.Category>
@{ ViewBag.Title = "Edit"; }

<h2>Edit Course Categories</h2>


@*Hien thi thong bao khi bi loi*@
@Html.ValidationSummary(false, "", new { @class = "bg-danger" })

@using (Html.BeginForm("Edit", "Todoes", FormMethod.Post))
{


    //gia tri thuoc tinh an</div>

@Html.HiddenFor(m => m.Category.Id)



                <div class="form-group">
                    @Html.LabelFor(m => m.Category.DueDate)
                    @Html.EditorFor(m => m.Category.DueDate,
                                    new { htmlAttributes = new { @class = "form-control", @type = "date" } })
                    @Html.ValidationMessageFor(m => m.Category.DueDate, "", new { @class = "text-danger" })

                </div>
                                <div class="form-group">
                                    <button type="submit" class="btn btn-default">Edit</button>
                                </div>
                                
                                
                                }