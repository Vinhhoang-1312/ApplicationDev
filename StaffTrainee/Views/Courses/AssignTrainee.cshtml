@model StaffTrainee.ViewModels.EnrollmentTraineeViewModel
@{
    ViewBag.Title = "AsignTrainee";
}

<h2>Asign Trainee To Course</h2>

@Html.ValidationSummary(false, "", new { @class = "bg-danger" })

@using (Html.BeginForm("AsignTrainee", "Courses", FormMethod.Post))
{

    <div class="form-group">
        Username
        @Html.HiddenFor(m => m.CourseId)
        @Html.DropDownListFor(m => m.CourseId,
         new SelectList(Model.Users, "Id", "Username"),
         new { @class = "form-control" }
         )
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary">Add to Course</button>
    </div>
}